export interface IProfile {
    email: string;
    gender: string | null;
    id: string;
    name: string;
    phone: string | null;
    profilePicture: string | null;
    profilePictureType:string;
}